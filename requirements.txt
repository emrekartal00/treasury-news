# Install:  pip install -r requirements.txt
# Then (only if system Chrome is absent):  python -m playwright install chromium
playwright>=1.40
oracledb>=2.0
requests>=2.31
beautifulsoup4>=4.12   # HTML->text for ingest (stdlib html.parser backend; no C build)
# selectolax>=0.3       # optional: faster parser; ingest uses it if present, else bs4
